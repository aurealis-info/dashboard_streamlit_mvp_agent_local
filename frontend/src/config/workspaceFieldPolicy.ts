/**
 * Add organization-approved portal status labels here, one per line.
 * When this list is empty, portal status remains free text because the
 * architecture guide does not define a controlled vocabulary.
 */
export const portalStatusOptions: string[] = [
  // 'Approved status',
]
